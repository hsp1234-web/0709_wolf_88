# This file makes the 'strategies' directory a Python package.
# You can optionally import strategy functions here for easier access, e.g.:
# from .sma_cross_strategy import generate_signals as sma_cross_generate_signals

# Or, leave it empty if dynamic loading (e.g., using importlib) is preferred
# without explicit imports here.
