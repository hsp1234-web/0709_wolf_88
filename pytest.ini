[pytest]
markers =
    live: 實彈測試，會對外部服務發起真實請求。
    asyncio: for asyncio marker
# 如果還有其他自訂標記，也可以一併註冊

python_files = test_*.py
python_classes = Test*
python_functions = test_*

norecursedirs = .git .* apps data output scripts temp tools venv _test_harness_*.py
testpaths = tests/unit
